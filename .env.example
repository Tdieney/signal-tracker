# VN Stock Signal — Environment Configuration Template
# NOTE: No secrets should ever be placed in frontend code or committed to this repository.

# Public Configuration (used at build time or local dev)
PUBLIC_APP_TITLE="VN Stock Signal"
PUBLIC_BASE_URL="/"
DATA_PROVIDER="csv"

# Pipeline optional settings (Server-side / CI only)
# DATA_FETCH_TIMEOUT_SECONDS=30
# MAX_RETRY_ATTEMPTS=3
